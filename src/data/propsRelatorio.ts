import { ReactElement } from "react";

interface IPropsRelatorio {
    titulo: string;
    descricao: string;
    icon: ReactElement;
}

export const propsRelatorio: IPropsRelatorio = [
    {

    }
]

